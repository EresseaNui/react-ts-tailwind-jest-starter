/**
 * L'addition est une fonction qui prend deux nombres et renvoie la somme de ces deux nombres.
 * @param {number} a - nombre - Le premier nombre à ajouter
 * @param {number} b - nombre - C'est le deuxième paramètre, et c'est un nombre.
 * @returns Une fonction
 */
export const Addition = (a: number, b: number) => {
    return a + b;
};
